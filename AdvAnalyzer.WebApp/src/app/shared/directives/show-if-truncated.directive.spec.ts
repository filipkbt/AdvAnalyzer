import { ShowIfTruncatedDirective } from './show-if-truncated.directive';

describe('ShowIfTruncatedDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowIfTruncatedDirective();
    expect(directive).toBeTruthy();
  });
});
